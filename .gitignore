node_modules/
cache/
artifacts/
typechain-types/
coverage/
coverage.json
.env
*.log
out.txt
apps/mobile/dist/
.expo/

# Android signing — NEVER commit keystores or their passwords
*.keystore
*.jks
keystore.properties
apps/mobile/credentials.json
apps/mobile/android/app/*.keystore
apps/mobile/android/app/*.jks
secrets/

# Per-operator deployment records + generated production manifest (addresses are
# public, but this is environment-specific output — keep the repo demo-mode)
deployments/
config/mobile-app.manifest.production.json
config/mobile-app.manifest.generated.json

# Secrets
.env
.env.local
.env.production
.env.development
credentials.json
google-services.json
service-account.json
